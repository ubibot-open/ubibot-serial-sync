build/
build-*/
deploy/
*.user
*.user.*
CMakeLists.txt.user
.qtc_clangd/
.vs/
out/
*.qm
build.md

# Local device-library update-server config (see .env.example and
# docs/device-library-update-protocol.md) -- deliberately never committed,
# since the URL/key can differ per environment.
.env
