# Template for the device-command-library update server. Copy this file to
# ".env" (same directory) and fill in real values -- ".env" itself is
# gitignored and never committed, since the URL/key may differ per
# environment (dev/staging/prod) and shouldn't require a rebuild to change.
# See docs/device-library-update-protocol.md for the full protocol.
#
# Where the app looks for ".env" at runtime, in order:
#   1. next to the built executable (production/deploy layout)
#   2. the current working directory (dev convenience)
#   3. this checkout's own source root (dev convenience -- lets a ".env" at
#      the repo root be found even when running from Qt Creator, whose
#      default working directory is the build dir, not the source dir)
# Not finding it (or leaving DEVICE_LIBRARY_API_BASE_URL blank) just disables
# remote updates -- the app falls back to the bundled resources/devices.json,
# nothing crashes.

# Base URL the app calls for the two device-library endpoints:
#   GET {DEVICE_LIBRARY_API_BASE_URL}/version  (cheap "is there an update?" check)
#   GET {DEVICE_LIBRARY_API_BASE_URL}/latest   (full devices.json-shaped payload)
# No trailing slash needed (the app strips one if present). The real backend
# (ubibot-appcenter's Laravel app) registers these two under
# /api/device-library/*, so a real base URL includes that path -- only the
# local Go mock server (see server/) defaults to a bare host:port unless you
# include the same suffix, per its own README.
DEVICE_LIBRARY_API_BASE_URL=http://localhost:8980/api/device-library

# Optional. When non-empty, sent as the "X-Api-Key" header on every request.
# Leave blank if the backend doesn't require one.
DEVICE_LIBRARY_API_KEY=

# Base URL for the app's own "Check for software update" (Help menu) -- see
# docs/app-self-update.md. This is the *API root*, not a product-specific
# path -- SoftwareUpdateClient appends "/software/list" itself. Reuses
# ubibot-appcenter's existing general-purpose software/version distribution
# system (the same one other UbiBot products use), not a bespoke endpoint,
# so this is typically the same host as DEVICE_LIBRARY_API_BASE_URL above,
# just its bare /api root instead of a specific sub-path. Leaving this blank
# disables the whole feature -- the Help menu item just reports "no update
# server configured", nothing crashes.
APP_UPDATE_API_BASE_URL=http://localhost:8990/api

# Optional, same idea as DEVICE_LIBRARY_API_KEY above -- sent as "X-Api-Key"
# if non-empty. The reference GET /api/software/list endpoint doesn't
# currently check for one either way.
APP_UPDATE_API_KEY=
